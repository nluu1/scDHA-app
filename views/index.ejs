<!DOCTYPE html>
<html>

<head>
    <title>Hello, World!</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <!-- Include Bootstrap CSS via CDN -->
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="col-md-6 offset-md-3 mt-5">
        <h1>scDHA</h1>
        <form accept-charset="UTF-8" action="/upload" method="POST" enctype="multipart/form-data">
            <div class="form-group mt-3">
                <label class="mr-2">Upload single-cell RNA-seq matrix:</label>
                <input type="file" name="file">
            </div>
            <hr>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>

    <!-- Display the generated PNG image -->
    <div class="col-md-6 offset-md-3 mt-5">
        <div class="container">
            <% if (imagePath) { %>
            <!-- Display the image if imagePath is defined -->
            <img class="img-fluid" src="data:image/jpeg;base64,<%= imagePath.toString('base64') %>" alt="Uploaded Image">
            <% } else { %>
            <!-- Display a placeholder or message if imagePath is not defined -->
            <p>No clustering result available.</p>
            <% } %>
        </div>

    </div>
    <script src="/js/script.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>